services:
  - type: web
    name: devops-demo
    env: node
    plan: free
    buildCommand: npm ci --only=production
    startCommand: node app.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
